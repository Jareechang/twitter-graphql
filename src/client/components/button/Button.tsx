import {
  Button as MuiButton
} from '@mui/material';

import styled from 'styled-components';

const Button = styled(MuiButton)`
  border-radius: 4rem;
  padding: 0.5rem 2rem;
`;

export default Button;
